//mutable

main(){
    var y = []; // empty list
    var x = [1,2,3];
    List<int> numbers = [1,2,3,4]; // for specifiying type

    x.add(4); // to append to the list
    x.remove(2); // to remove the element from the list 
    var listLength = x.length;
    var listIsEmpty = x.isEmpty;
}